\section{Thiết lập cụm Pseudo-distributed}
\label{sec:pseudo-distributed}

% Mỗi thành viên tự viết 1 subsection riêng (Transcript §5)
% Cài đặt trực tiếp trên hệ điều hành, KHÔNG dùng image có sẵn (FAQ Q3)
% Rubric: "Successfully install Hadoop (NameNode, DataNode)" = 1.0 pt
% Rubric: "Detailed report (setup, step-by-step, errors)" = 1.0 pt

\subsection{Sinh viên: Lưu Trọng Hiếu --- 23120258}

\subsubsection{Phương thức cài đặt}
% Ghi rõ: Ubuntu Native / WSL / VM — phương thức nào, OS nào

\subsubsection{Quá trình cài đặt}
% Step-by-step kèm screenshot
% Bao gồm: tải Hadoop, cấu hình XML, format NameNode, start-dfs.sh, jps

\subsubsection{Xử lý sự cố}
% Ghi nhận lỗi gặp phải → Google search → cách sửa → kết quả sau khi sửa

% ===========================================================================
% THÊM THÀNH VIÊN: Copy khối \subsection{} bên trên, đổi tên + MSSV
% \subsection{Sinh viên: <Họ tên> --- <MSSV>}
% ===========================================================================
