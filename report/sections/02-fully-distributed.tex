\section{Thiết lập cụm Fully-distributed}
\label{sec:fully-distributed}

% Nhóm n người cần n-1 lượt cài đặt fully-distributed (Lab PDF)
% Có thể trùng phương thức: VM / physical machines (FAQ Q4)
% Rubric: "YARN on VMs and physically distributed machines" = 1.5 pt
% Mỗi lượt cài đặt = 1 subsection

\subsection{Lượt 1 — Sinh viên: Lưu Trọng Hiếu --- 23120258}

\subsubsection{Phương thức}
% VM (Docker containers / VirtualBox / VMWare) hay Physical machines?
% Tối thiểu 2 workers để được điểm bonus

\subsubsection{Cấu hình hệ thống}
% Kiến trúc: NameNode, ResourceManager, DataNode x N
% Cấu hình: core-site.xml, hdfs-site.xml, yarn-site.xml, workers

\subsubsection{Khởi tạo và xác minh}
% start-dfs.sh, start-yarn.sh, jps trên từng node
% Screenshot xác nhận tất cả nodes hoạt động

\subsubsection{Xử lý sự cố}
% Lỗi → tra cứu → sửa

% ===========================================================================
% THÊM LƯỢT CÀI ĐẶT: Copy khối \subsection{} bên trên
% \subsection{Lượt 2 — Sinh viên: <Họ tên> --- <MSSV>}
% ===========================================================================
